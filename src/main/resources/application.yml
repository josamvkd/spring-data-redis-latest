spring:
  application:
    name: spring-data-redis-latest

  data:
    redis:
      host: localhost
      port: 6379
      # password: <your_password>   # uncomment if Redis requires auth

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include: health, info, metrics